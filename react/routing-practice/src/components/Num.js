import React from 'react'
import { Link } from '@reach/router';


const Num = (props) => {

    return (
        <>
          if ( isNaN({props.num})) { <h1>The number is {props.num}</h1> }else{
            // <h2 style={{ display: "flex", width: "650px", paddingLeft: " 580px" }} >{props.word}</h2>
        false 
        }


            <div style={{
                background: "green", color: "red", fontWeight: "bold", height: "20px"
            }}>
                <Link to="/" >EMPTY</Link>
            </div>

        </>
    )

}

export default Num